/**
 * Адрес бэкенда. Единственное место, где он задан.
 *
 * Порт должен совпадать с PORT сервера (server-v2/src/config/config.ts).
 * По умолчанию там 3500, а не 3000: 3000 часто занят другими проектами.
 */
export const API_URL = 'http://localhost:3500/api';
